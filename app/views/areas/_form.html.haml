= form_for([@questionario, @area]) do |f| 
	= render 'shared/error_messages', object: f.object
	.form-group.has-success
		= f.text_field :nome, :class => "form-control"
	= f.submit, :class => "btn btn-success btn-sm form-control"
