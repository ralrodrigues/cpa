<h2>Novo CPA</h2>

<%= form_for(@questionario) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <p>
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </p>

  <p>
    <%= f.submit "Cadastrar" %>
    <%= link_to 'Cancelar', questionarios_path %>
  </p>
<% end %>